
function destroyer(arr) {
  // Remove all the values
  var destroyedArray = [];
  for (var i = 1; i < arguments.length; i++) {
    destroyedArray.push(arguments[i]);
  }
  
  return arr;
}

destroyer([1, 2, 3, 1, 2, 3], 2, 3);
